<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/tract_list"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:layout_editor_absoluteX="16dp"
        tools:listitem="@layout/fragment_tract_list_item">

    </androidx.recyclerview.widget.RecyclerView>

    <ImageView
        android:id="@+id/no_tract_image"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:src="@drawable/ic_kta_tract_no_tract___essai_1"
        app:layout_constraintBottom_toTopOf="@+id/no_tract_button"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        tools:srcCompat="@drawable/ic_kta_tract_no_tract___essai_1" />

    <Button
        android:id="@+id/no_tract_button"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="16dp"
        android:background="@color/transparent"
        android:drawableBottom="@drawable/ic_add_icon"
        android:drawableTint="@color/black"
        android:text="@string/no_tract_list"
        android:textColor="@color/black"
        app:layout_constraintBottom_toBottomOf="@+id/tract_list" />

</androidx.constraintlayout.widget.ConstraintLayout>